# Pin the Node version pnpm uses to run scripts, so `pnpm start` (and the
# native better-sqlite3 binary) always run under the Node ABI they were
# built against, regardless of the shell's default Node.
use-node-version=26.1.0
# Refuse to install/run under a Node that doesn't satisfy package.json engines.
engine-strict=true
